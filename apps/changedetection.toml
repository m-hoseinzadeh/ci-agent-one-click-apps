slug = "changedetection"
name = "ChangeDetection.io"
category = "Monitoring"
description = "ChangeDetection.io is the self-hosted website change detection monitoring."
keywords = ["changedetection", "changedetection.io"]
http_port = 5000
compose = '''
services:
  changedetection:
    image: dgtlmoon/changedetection.io:{{VERSION}}
    environment:
      BASE_URL: "{{APP_URL}}"
    volumes:
    - changedetection-data:/datastore
    ports:
    - '{{HTTP_PORT}}:5000'
volumes:
  changedetection-data:
'''

[[variables]]
key = "APP_URL"
label = "Public base URL"
default = ""
description = "Optional. Public base URL of this app (e.g. https://changedetection.example.com), used to build links in change notifications. Leave blank to set later."

[[variables]]
key = "VERSION"
label = "ChangeDetection.io version"
default = "0.40.2"
description = "Check out their docker page for the valid tags https://hub.docker.com/r/dgtlmoon/changedetection.io/tags"

slug = "n8n-io-sqlite"
name = "n8n.io (SQLite)"
category = "Database"
description = "Node based workflow automation tool"
keywords = ["(sqlite)", "io", "n8n", "n8n.io", "sqlite"]
http_port = 5678
compose = '''
services:
  n8n-io-sqlite:
    image: n8nio/n8n:{{N8N_VERSION}}
    environment:
      GENERIC_TIMEZONE: '{{N8N_TIMEZONE}}'
      TZ: '{{N8N_TIMEZONE}}'
      NODE_ENV: '{{N8N_ENVIRONMENT}}'
      N8N_DIAGNOSTICS_ENABLED: '{{N8N_DIAGNOSTICS_ENABLED}}'
      N8N_EMAIL_MODE: smtp
      N8N_SMTP_HOST: '{{N8N_SMTP_HOST}}'
      N8N_SMTP_PORT: '{{N8N_SMTP_PORT}}'
      N8N_SMTP_USER: '{{N8N_SMTP_USER}}'
      N8N_SMTP_PASS: '{{N8N_SMTP_PASS}}'
      N8N_SMTP_SENDER: '{{N8N_SMTP_SENDER}}'
      N8N_SMTP_SSL: '{{N8N_SMTP_SSL}}'
      WEBHOOK_URL: '{{APP_URL}}'
      N8N_EDITOR_BASE_URL: '{{APP_URL}}'
    volumes:
    - n8n-io-sqlite:/home/node/.n8n
    ports:
    - '{{HTTP_PORT}}:5678'
volumes:
  n8n-io-sqlite:
'''

[[variables]]
key = "APP_URL"
label = "Application | Public URL (optional)"
default = ""
description = "Public base URL of this n8n instance, e.g. https://n8n.example.com. Set after assigning a domain; used for webhooks and the editor base URL. Leave blank until a domain is assigned."

[[variables]]
key = "N8N_DIAGNOSTICS_ENABLED"
label = "Application | Enable Telemetry"
default = "false"
description = "Whether to send telemetry data to n8n.io."

[[variables]]
key = "N8N_ENVIRONMENT"
label = "Application | Environment"
default = "production"
description = "Application environment."

[[variables]]
key = "N8N_SMTP_HOST"
label = "Application | SMTP Host (optional)"
default = ""
description = "SMTP host used for sending mails. External credential; leave blank if unused."

[[variables]]
key = "N8N_SMTP_PASS"
label = "Application | SMTP Password (optional)"
default = ""
description = "SMTP user password used for sending mails. External credential; leave blank if unused."

[[variables]]
key = "N8N_SMTP_PORT"
label = "Application | SMTP Port (optional)"
default = ""
description = "SMTP port used for sending mails. Usually `465`."

[[variables]]
key = "N8N_SMTP_SENDER"
label = "Application | SMTP Sender (optional)"
default = ""
description = "SMTP sender used for sending mails (e.g. `N8N <n8n@example.com>`)."

[[variables]]
key = "N8N_SMTP_SSL"
label = "Application | SMTP SSL"
default = "true"
description = "Whether to use SSL for sending mails through SMTP."

[[variables]]
key = "N8N_SMTP_USER"
label = "Application | SMTP User (optional)"
default = ""
description = "SMTP user used for sending mails. External credential; leave blank if unused."

[[variables]]
key = "N8N_TIMEZONE"
label = "Application | Timezone"
default = "UTC"
description = "Timezone used by the server. Please check out this page for valid time zones: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones"

[[variables]]
key = "N8N_VERSION"
label = "Application | n8n.io"
default = "1.77.0"
description = "Check out their Docker page for the valid tags https://hub.docker.com/r/n8nio/n8n/tags"
